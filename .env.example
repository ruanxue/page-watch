PORT=3030
PLAYWRIGHT_HEADLESS=true
OUTBOUND_PROXY=
PLAYWRIGHT_EXECUTABLE_PATH=
APP_SESSION_SECURE=false

MYSQL_HOST=192.168.100.186
MYSQL_PORT=3306
MYSQL_DATABASE=page_watch
MYSQL_USER=page_watch
MYSQL_PASSWORD=
# 单容器内六个 Node 进程各自的 MySQL 连接上限；通常保持 3 即可。
MYSQL_CONNECTION_LIMIT=3

# 32-byte Base64URL key for encrypted external-service credentials. Generate once with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
APP_ENCRYPTION_KEY=
